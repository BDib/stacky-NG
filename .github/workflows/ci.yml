name: GitHub CI Build

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ] 
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        arch: [x64, Win32, ARM64]
        config: [Debug, Release]
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup MSVC Environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch }}

      - name: Configure CMake
        run: cmake -B build -S . -A ${{ matrix.arch }} -DCMAKE_BUILD_TYPE=${{ matrix.config }}

      - name: Build
        run: cmake --build build --config ${{ matrix.config }}

      - name: Rename and Upload Artifacts
        shell: powershell
        run: |
          $finalName = "stacky-${{ matrix.arch }}-${{ matrix.config }}.exe"
          Move-Item -Path "build/${{ matrix.config }}/stacky.exe" -Destination $finalName
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: stacky-${{ matrix.arch }}-${{ matrix.config }}
          path: stacky-${{ matrix.arch }}-${{ matrix.config }}.exe
          
  build-gui:
    runs-on: windows-latest
    strategy:
      matrix:
        rid: [win-x64, win-x86, win-arm64]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Build and Zip GUI Tool
        shell: powershell
        run: |
          $TargetDir = "dist/Tools/Setup_Stacky/${{ matrix.rid }}"
          
          # 1. Publish (Matches BuildTools.ps1 logic)
          dotnet publish Tools/GUI/Setup_Stacky/Setup_Stacky.csproj -c Release -r ${{ matrix.rid }} --self-contained true -p:PublishSingleFile=true -p:PublishReadyToRun=false -o $TargetDir
          
          # 2. Compress (Creates the zip archive)
          $ZipPath = "Setup_Stacky-${{ matrix.rid }}.zip"
          Compress-Archive -Path "$TargetDir/*" -DestinationPath $ZipPath -Force
          
          # Set an output for the next step to find the zip
          echo "ZIP_PATH=$ZipPath" >> $env:GITHUB_ENV

      - name: Upload GUI Artifact
        uses: actions/upload-artifact@v4
        with:
          name: gui-tool-${{ matrix.rid }}
          path: ${{ env.ZIP_PATH }}

  build-stacky-manager:
    runs-on: windows-latest
    strategy:
      matrix:
        arch: [x64, Win32, ARM64]
        config: [Debug, Release]
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Setup MSVC Environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch }}

      - name: Build StackyManager
        shell: powershell
        run: |
          cd Tools\GUI\StackyManager
          .\Build.ps1 -Arch ${{ matrix.arch }} -Config ${{ matrix.config }}

      - name: Zip Artifacts
        shell: powershell
        run: |
          # Define the folder created by your script
          $SourceDir = "Tools/GUI/StackyManager/Bin/${{ matrix.arch }}"
          $ZipName = "StackyManager-${{ matrix.arch }}-${{ matrix.config }}.zip"
          
          # Zip the entire Bin architecture folder
          Compress-Archive -Path "$SourceDir/*" -DestinationPath $ZipName -Force
          
          # Set as env var for the next step
          echo "ZIP_PATH=$ZipName" >> $env:GITHUB_ENV

      - name: Upload Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: stacky-manager-${{ matrix.arch }}-${{ matrix.config }}
          path: ${{ env.ZIP_PATH }}

  release:
    needs: [build, build-gui, build-stacky-manager]
    runs-on: windows-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true 

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*